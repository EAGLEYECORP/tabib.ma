'use client';
import { useParams } from 'next/navigation';
import { useState } from 'react';
export default function CancelPage(){const {id}=useParams<{id:string}>();const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');async function cancel(){setBusy(true);setMsg('');try{const r=await fetch('/api/appointments/cancel',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({appointment_id:id})});const j=await r.json();if(!r.ok){setMsg(j.error||'Impossible d’annuler');return;}window.location.href='/account/appointments';}finally{setBusy(false)}}return <main className="wrap"><div className="card"><h1>Annuler le rendez-vous ?</h1><p className="muted">Cette action annule la réservation sélectionnée.</p><div style={{display:'flex',gap:10}}><button className="btn" onClick={cancel} disabled={busy}>{busy?'Annulation…':'Oui, annuler'}</button><button className="btn secondary" onClick={()=>history.back()}>Retour</button></div>{msg&&<p role="alert">{msg}</p>}</div></main>}
