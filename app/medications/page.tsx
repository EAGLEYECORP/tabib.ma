'use client';
import {useState} from 'react';
export default function MedicationsPage(){const[q,setQ]=useState(''),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(false);async function search(){setLoading(true);try{const r=await fetch(`/api/medications/search?q=${encodeURIComponent(q)}`,{cache:'no-store'});const j=await r.json();setRows(j.results||[])}finally{setLoading(false)}}return <main className="container"><h1>Catalogue des médicaments</h1><p>Référentiel informatif issu de sources autorisées. Ce catalogue ne constitue pas une recommandation médicale.</p><div style={{display:'flex',gap:8}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Nom du médicament" maxLength={100}/><button onClick={search} disabled={loading||q.trim().length<2}>Rechercher</button></div><div style={{marginTop:24}}>{rows.map(r=><article key={r.id} style={{padding:'14px 0',borderBottom:'1px solid #ddd'}}><strong>{r.name}</strong><div>{r.active_ingredient||''} {r.strength||''} · {r.pharmaceutical_form||''}</div><div>{r.manufacturer||''} · PPH: {r.pph_mad==null?'—':`${r.pph_mad} MAD`}</div></article>)}</div></main>}
